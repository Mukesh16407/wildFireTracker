import googleMapReact from "google-map-react";
import { React } from "react";

export const Map = ({ center, zoom }) => {
  return (
    <div className="map">
      <googleMapReact>bootstrapUrlKeys=</googleMapReact>
    </div>
  );
};
